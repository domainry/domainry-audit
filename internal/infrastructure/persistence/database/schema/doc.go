// Package schema marks the source-owned Audit persistence schema boundary.
// Executable definitions are versioned by the sibling migration package, while
// physical type profiles live below persistence/<dialect>/schema.
package schema
